Place the offline Three.js r128 browser build in this directory:

- `three.min.js`
- `GLTFLoader.js` from the matching r128 examples build

The avatar page intentionally references only these local files so it can run without a CDN.
